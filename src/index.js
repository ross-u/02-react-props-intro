import React from "react";
import ReactDOM from "react-dom";
import App from "./App.js";

// index.js is used to inject the react app to the DOM
ReactDOM.render(<App />, document.getElementById("root"));
